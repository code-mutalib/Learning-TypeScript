# TypeScript Practice Repository

This repository is a structured practice workspace for learning and strengthening **TypeScript fundamentals**.  
It is designed to build a solid foundation before using TypeScript in backend or full-stack applications.

---

## 📌 Purpose

The goal of this repository is to:

- Learn core TypeScript syntax and concepts
- Understand TypeScript’s type system
- Practice writing type-safe functions and variables
- Learn module imports and exports
- Get comfortable with `tsc` and `ts-node`
- Prepare for real-world TypeScript usage

This repository focuses on **learning**, not frameworks.

---

## 📂 Project Structure

```txt
TypeScript/
│
├── src/
│   ├── index.ts          # Entry / runner file
│   ├── basic.ts          # TypeScript basics
│   ├── typesInTS.ts      # Type system examples
│
├── dist/                 # Compiled JavaScript output (ignored by git)
│
├── package.json
├── tsconfig.json
├── pnpm-lock.yaml
├── .gitignore
└── README.md
